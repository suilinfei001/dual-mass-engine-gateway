背景：
现有代码目录有点混乱，不能满足产品架构的梳理。请帮忙改造

产品架构梳理：
1. 本项目旨在搭建一套双引擎质量网关的系统来保证github开源项目的代码质量。
2. 只要是2个部分，第一个部分是事件接收器，另一个是事件处理器。
3. 事件接收器的主要作用是提供了webhook，可以接受github action发送的主线push事件和目标是主线的PR事件。还实现了初始化质量卡点的创建，这些都记录在了mysql数据库中。除此之外还实现了一个网站可以查看所有的事件及其质量卡点的状态。
4. 事件接收器的开发已经完成，当前代码就是事件接受器的实现。目前已经部署在10.4.111.141环境。
5. 事件接受器需要和github通讯，由于网络环境受限，所以它是单独部署在外网环境，使用intall_quality.sh实现部署。
6. 事件处理器和事件接收器共享一个目录仓库，但会使用不同的部署脚本进行部署，并且事件处理器是部署在内网，事件处理器的服务器只可以跟事件接受器通过restapi沟通，无法直接访问github等外部网站。
7. 事件接受器的代码还没有开始开发，请先重构现在的代码结构，例如可以在src目录下分别管理2个功能模块的代码

要求：
1. 这个是一个前后端+数据库全栈项目，且有2个模块需要独立部署在不同服务器上，都有各自的前后端，以及独立的部署方式。
2. 测试脚本归类在tests目录下，安装脚本归类在install目录下，2个模块分开管理

基于以上信息，先帮忙改造吧